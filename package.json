{
  "name": "shadowmesh-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "setup": "npm install && cd apps/api && npm install && cd ../web && npm install",
    "dev": "concurrently \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/web\"",
    "build": "cd apps/api && npm run build && cd ../web && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}